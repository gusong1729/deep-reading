# deep-reading 阅读精读领域

孤松的全格式阅读精读体系：输入任意格式文件（PDF/EPUB/TXT/DOCX/MD/HTML，含扫描件 OCR），产出详尽完整的全文精读文档供用户阅读，用户读后通过答疑环节深化理解，所有精读与问答经由卡帕西 LLM Wiki 机制沉淀进 Obsidian 知识库。

## Language

**精读（Deep Reading）**:
对输入文件产出的详尽完整解读文档，是 skill 的主产品。精读必须可独立阅读：用户读它即可理解原文，且深度允许调用 anysearch 检索外部学者评价融入。
_Avoid_: 摘要、总结、导读

**精读模板（Reading Template）**:
按方向模块定义的精读产出格式。当前两型：`逐段精解`（古文，原文+白话+深层解读+即时注释）与 `结构化解读`（哲学/电工，脉络+论证骨架+关键段细读+概念提取）。
_Avoid_: 单一模板、自由格式

**答疑（Q&A）**:
用户读完精读后提问、agent 按 Query 模式（读 index → 定位页面 → 综合回答）回答的环节。有价值的问答必须反哺 wiki。
_Avoid_: 一次性聊天、不落库的问答

**方向模块（Direction Module）**:
学科阅读策略适配器，决定精读模板选型与概念页字段结构。通过注册机制动态扩展。
_Avoid_: 硬编码学科、写死三个方向

**通用模式（Generic Mode）**:
未匹配方向模块时使用的默认精读策略（结构化解读型）。允许在无模块时正常服务，并提示事后沉淀新模块。

**Raw 层**:
卡帕西知识库第一层。原始资料（原文文件、扫描件、网页剪藏），LLM 只读不写。
_Avoid_: 修改原始资料、在 raw 层做整理

**Wiki 层**:
卡帕西知识库第二层。LLM 全权维护的 Markdown 知识页（概念页、摘要页、索引），交叉链接，新资料触发关联页增量更新。
_Avoid_: 用户手工编辑 wiki、把 wiki 当草稿本

**Schema 层**:
卡帕西知识库第三层。规则文件（SCHEMA.md），约束 wiki 的结构规范、命名约定、工作流。与用户共同迭代。
_Avoid_: schema 与 SKILL.md 职责混淆、schema 静置不更新

**Ingest**:
摄入操作：新资料 → 摘要 → 更新所有关联页 → 写 index 与 log。每个精读产出必须执行一次 Ingest。
_Avoid_: 只写新页不更新关联页、跳过 index/log

**Query**:
查询操作：先读 index.md 定位 → 深入相关页 → 综合回答。好答案存回 wiki 成为新页。
_Avoid_: 跳过索引直接翻库、答案不落库

**Lint**:
体检操作：定期检查矛盾数据、孤立页、未建页概念、数据缺口，并建议下一步研究方向。

**概念页（Concept Page）**:
wiki 中描述单个概念/实体的页面。必含来源引用与跨域 domain 标签，同名概念跨书合并。
_Avoid_: 无来源引用、单域隔离不互联

**index.md**:
知识库内容索引，每个 wiki 页面一行（链接+一句话摘要+分类标签）。回答任何问题前必须先读。
_Avoid_: 索引缺失、索引与页面不同步

**log.md**:
知识库操作日志，按时间记录每次 ingest/query/lint 操作（`## [YYYY-MM-DD] ingest | 标题`），grep 可回溯。
_Avoid_: 不写操作记录、日志格式不一

## Rules

- **精读是主产品**：答疑、知识库都是精读的延伸，所有环节为"用户读得懂、学得深"服务。
- **Be opinionated**：术语歧义时选一个标准词，其余列入 _Avoid_。
- **Schema 是契约**：wiki 结构、精读模板、方向模块都以 Schema 层为唯一权威，SKILL.md 执行、SCHEMA.md 定规。
- **LLM 是作者，用户是主编**：wiki 内容由 LLM 产出，但来源引用强制、用户定期抽查验证。
- **方向模块可扩展**：新增学科通过"通用模式兜底 + 事后沉淀"机制注册，不修改核心流程。
