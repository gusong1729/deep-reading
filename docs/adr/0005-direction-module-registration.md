# ADR-0005：方向模块注册机制 = 手动优先 + 自动辅助 + 通用兜底 + 事后沉淀

方向模块选中机制：用户手动声明优先（"读这本书，用 X 方向"），agent 从文件名/内容特征自动嗅探辅助；无匹配模块时先用通用模式精读，读完提示"是否把本次阅读沉淀为新方向模块"。

背景：纯自动嗅探在古文上不可靠（《文心雕龙》文本特征与哲学论文极像）；纯手动声明增加用户负担。C 方案让新学科第一次读先跑通通用框架，体验不顺时现场固化成新模块，支撑"全覆盖"目标且不阻塞任何一次阅读。
