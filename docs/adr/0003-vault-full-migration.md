# ADR-0003：Obsidian Vault 全库改造纳入 LLM Wiki 体系

用户选择全库改造（推翻 agent 推荐的"先建阅读域、验证后再扩"局部方案）：整个 `D:\软件\Obsidian\Vault\Obsidian Vault` 一次性纳入 LLM Wiki 架构。

背景：局部方案的动机是控制风险；用户判断现有库体量小（31 个 md 文件）、手工维护的 wiki 本就打算另起炉灶（"利润太少"），一次性改造的迁移成本可接受，且能避免新旧两套体系长期并存。改造前须出完整迁移方案保证可逆。
